using NodeGraphProcessor;
using NodeGraphProcessor.Editor;
using UnityEditor;

public class CustomToolbarGraphView : BaseGraphView
{
	public CustomToolbarGraphView(EditorWindow window)
		: base(window) {}
}